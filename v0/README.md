# v0 — original artifacts

Legacy, pre-refactor. Kept for reference only.

- `Quarry Analyst (standalone).html` — the original compiled single-file bundle.
- `AI Data Analysis Platform - from Claude Design.zip` — the original Claude Design export.

The live app is built from `src/` via `build.py` — see the root README.
